# Multi-Tenant Architecture - Документация

> **Важно:** Этот документ содержит детали реализации multi-tenant функциональности. При работе с этим функционалом добавляйте сюда заметки и изменения.

## 📋 Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Реализованные функции](#реализованные-функции)
3. [Безопасность](#безопасность)
4. [Известные ограничения](#известные-ограничения)
5. [Заметки по разработке](#заметки-по-разработке)

---

## Обзор архитектуры

### Концепция

Приложение использует **multi-tenant архитектуру**, где каждая организация имеет изолированные данные. Все записи в базе данных связаны с организацией через поле `organizationId`.

### Основные компоненты

- **`src/lib/organization-context.ts`** - Утилиты для работы с организацией

  - `getCurrentOrganizationId()` - Получает ID текущей организации
  - `ensureOrganizationId()` - Получает или создает организацию

- **База данных:** Все таблицы имеют колонку `organizationId` с внешним ключом на `Organization`

---

## Реализованные функции

### ✅ Приоритет 1 (Критично - безопасность)

#### 1. Проверка organizationId в UPDATE операциях

**Статус:** ✅ Реализовано

Все UPDATE endpoints проверяют принадлежность записи к организации перед обновлением:

```typescript
// Пример из /api/clients/[id]/route.ts
const existing = await prisma.client.findFirst({
	where: { id, organizationId },
})
if (!existing) throw new ApiError(404, 'Client not found')
```

**Затронутые endpoints:**

- `/api/clients/[id]` (PUT)
- `/api/clients` (PUT)
- `/api/suppliers` (PUT)
- `/api/partners` (PUT)
- `/api/installers` (PUT)
- `/api/proposals/[id]` (PUT)

#### 2. Проверка organizationId в DELETE операциях

**Статус:** ✅ Реализовано

Все DELETE endpoints проверяют принадлежность записи к организации перед удалением:

```typescript
// Пример из /api/clients/[id]/route.ts
const existing = await prisma.client.findFirst({
	where: { id, organizationId },
})
if (!existing) throw new ApiError(404, 'Client not found')
```

**Затронутые endpoints:**

- `/api/clients/[id]` (DELETE)
- `/api/clients` (DELETE)
- `/api/suppliers` (DELETE)
- `/api/partners` (DELETE)
- `/api/installers` (DELETE)
- `/api/proposals/[id]` (DELETE)

#### 3. Фильтрация по organizationId в GET по ID

**Статус:** ✅ Реализовано

Все GET endpoints по ID фильтруют данные по organizationId:

```typescript
// Пример из /api/clients/[id]/route.ts
const client = await prisma.client.findFirst({
	where: { id, organizationId },
})
```

**Затронутые endpoints:**

- `/api/clients/[id]` (GET)
- `/api/proposals/[id]` (GET)

#### 4. Запрет изменения organizationId через UPDATE

**Статус:** ✅ Реализовано

Все `build*UpdateData` функции **не включают** `organizationId` в данные для обновления:

```typescript
// Пример из /api/clients/helpers.ts
export function buildClientUpdateData(input: ClientUpdateInput) {
	const data: Prisma.ClientUpdateInput = {}
	// ... другие поля
	// ❌ organizationId НЕ включается - он не должен изменяться!
	return data
}
```

**Проверено в:**

- `buildClientUpdateData`
- `buildSupplierUpdateData`
- `buildPartnerUpdateData`
- `buildInstallerUpdateData`
- `buildProposalUpdateData`

---

### ✅ Приоритет 3 (Улучшения)

#### 5. Валидация связанных данных

**Статус:** ✅ Реализовано

При создании и обновлении Proposal проверяется, что связанный Client принадлежит той же организации:

```typescript
// Пример из /api/proposals/helpers.ts
const client = await prisma.client.findFirst({
	where: { id: input.clientId, organizationId },
})
if (!client) {
	throw new ApiError(
		400,
		'Client not found or does not belong to your organization'
	)
}
```

**Реализовано в:**

- `buildProposalCreateData` - проверка Client при создании
- `/api/proposals/[id]` (PUT) - проверка Client при обновлении

#### 6. Фильтрация связанных данных в DELETE

**Статус:** ✅ Реализовано

При проверке связанных данных перед удалением учитывается organizationId:

```typescript
// Пример из /api/clients/[id]/route.ts
const proposalsCount = await prisma.proposalDocument.count({
	where: { clientId: id, organizationId },
})
```

**Реализовано в:**

- `/api/clients/[id]` (DELETE) - проверка Proposal и Order
- `/api/suppliers` (DELETE) - проверка ProposalPosition

---

## Безопасность

### Принципы безопасности

1. **Всегда проверяйте organizationId** перед любыми операциями с данными
2. **Никогда не доверяйте ID из запроса** - всегда проверяйте принадлежность
3. **Используйте `findFirst` с `where`** вместо `findUnique` для проверки organizationId
4. **organizationId не должен изменяться** через UPDATE операции

### Паттерн проверки

```typescript
// Стандартный паттерн для всех операций
const organizationId = await getCurrentOrganizationId()

const existing = await prisma.model.findFirst({
	where: {
		id,
		...(organizationId ? { organizationId } : {}),
	},
})

if (!existing) {
	throw new ApiError(404, 'Record not found')
}
```

---

## Известные ограничения

### ⚠️ Нет системы аутентификации пользователей

**Текущая реализация:**

- `getCurrentOrganizationId()` просто берет **первую организацию** из БД
- Нет API endpoints для регистрации/логина
- Нет получения `organizationId` из сессии пользователя
- Все пользователи видят данные одной организации

**Что уже есть:**

- ✅ Модель `User` в базе данных с полем `organizationId`
- ✅ Связь User → Organization в схеме Prisma

**Что нужно для полной реализации:**

1. **Регистрация пользователей:**

   - API endpoint `/api/auth/register`
   - При регистрации создавать пользователя + организацию
   - Связывать пользователя с созданной организацией

2. **Аутентификация:**

   - API endpoint `/api/auth/login`
   - JWT токены или cookies для сессии
   - Хранение `organizationId` в токене/сессии

3. **Обновление `getCurrentOrganizationId()`:**

   - Получать `organizationId` из сессии пользователя
   - А не просто первую организацию из БД

4. **Middleware:**
   - Проверка аутентификации перед доступом к API
   - Извлечение `organizationId` из сессии

**Статус:** ✅ Реализовано

**Реализованные функции:**

- ✅ Регистрация пользователей (`/api/auth/register`, `/auth/signup`)
- ✅ Аутентификация через NextAuth (`/auth/signin`)
- ✅ `getCurrentOrganizationId()` получает из сессии
- ✅ Middleware защищает все routes
- ✅ Приглашения пользователей (`/api/auth/invite`, `/auth/invite/[token]`)
- ✅ Отслеживание активности пользователей
- ✅ Очистка неактивных аккаунтов (`/api/cron/cleanup-inactive`)

---

### ⚠️ Старые записи имеют organizationId = null

**Проблема:** Существующие записи, созданные до реализации multi-tenant, имеют `organizationId = null`.

**Текущее поведение:**

- Записи с `null` игнорируются при фильтрации
- Новые записи автоматически получают `organizationId`

**Варианты решения:**

1. **Миграция данных:** Назначить все записи первой организации
2. **Оставить как есть:** Игнорировать записи с `null` (текущее поведение)
3. **Глобальные данные:** Некоторые записи могут быть общими для всех

**Рекомендация:** Решить стратегию для каждой таблицы отдельно.

---

## Заметки по разработке

### Добавление нового endpoint

При создании нового endpoint с операциями CRUD:

1. **GET список:**

```typescript
const organizationId = await getCurrentOrganizationId()
const items = await prisma.model.findMany({
	where: organizationId ? { organizationId } : undefined,
})
```

2. **GET по ID:**

```typescript
const organizationId = await getCurrentOrganizationId()
const item = await prisma.model.findFirst({
	where: { id, ...(organizationId ? { organizationId } : {}) },
})
if (!item) throw new ApiError(404, 'Not found')
```

3. **POST (создание):**

```typescript
const organizationId = await ensureOrganizationId()
const item = await prisma.model.create({
	data: { ...data, organizationId },
})
```

4. **PUT (обновление):**

```typescript
const organizationId = await getCurrentOrganizationId()
const existing = await prisma.model.findFirst({
	where: { id, ...(organizationId ? { organizationId } : {}) },
})
if (!existing) throw new ApiError(404, 'Not found')
const item = await prisma.model.update({
	where: { id },
	data: buildUpdateData(payload), // НЕ включать organizationId!
})
```

5. **DELETE:**

```typescript
const organizationId = await getCurrentOrganizationId()
const existing = await prisma.model.findFirst({
	where: { id, ...(organizationId ? { organizationId } : {}) },
})
if (!existing) throw new ApiError(404, 'Not found')
await prisma.model.delete({ where: { id } })
```

---

### Валидация связанных данных

При создании связей между записями всегда проверяйте принадлежность к организации:

```typescript
// Пример: проверка Client перед созданием Proposal
const client = await prisma.client.findFirst({
	where: { id: input.clientId, organizationId },
})
if (!client) {
	throw new ApiError(
		400,
		'Client not found or does not belong to your organization'
	)
}
```

---

## История изменений

### 2025-01-XX - Реализация базовой multi-tenant функциональности

- ✅ Добавлены колонки `organizationId` во все таблицы (18 таблиц)
- ✅ Созданы enum типы для всех статусов
- ✅ Реализована проверка organizationId в UPDATE/DELETE/GET операциях
- ✅ Добавлена валидация связанных данных
- ✅ Запрещено изменение organizationId через UPDATE

---

## TODO (Будущие улучшения)

### Приоритет 2 (Важно - функциональность) - КРИТИЧНО ДЛЯ РАБОТЫ!

**Без этого приложение НЕ работает как multi-tenant система!**

- [ ] **Реализовать регистрацию пользователей** (`/api/auth/register`)

  - При регистрации создавать новую организацию
  - Связывать пользователя с организацией
  - Возвращать JWT токен с `organizationId`

- [ ] **Реализовать аутентификацию** (`/api/auth/login`)

  - Проверка email/password
  - Создание JWT токена с `userId` и `organizationId`
  - Установка cookie с токеном

- [ ] **Обновить `getCurrentOrganizationId()`**

  - Получать `organizationId` из JWT токена/сессии
  - Убрать логику "первая организация из БД"

- [ ] **Реализовать middleware для проверки аутентификации**

  - Проверка JWT токена перед доступом к API
  - Извлечение `organizationId` из токена
  - Редирект на `/login` если не авторизован

- [ ] **Добавить логирование доступа к данным других организаций**

### Приоритет 3 (Улучшения)

- [ ] Решить стратегию для старых записей (null organizationId)
- [ ] Добавить тесты для multi-tenant безопасности
- [ ] Оптимизировать запросы с индексами на organizationId

---

## Контакты и вопросы

При работе с multi-tenant функциональностью:

1. Проверьте этот документ перед началом работы
2. Добавляйте заметки о найденных проблемах или улучшениях
3. Обновляйте раздел "История изменений" при внесении изменений
