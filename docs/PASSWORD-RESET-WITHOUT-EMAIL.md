# Восстановление пароля без email сервиса

**Дата:** 2025-12-06  
**Версия:** 1.4.0

---

## 📋 Описание

Система восстановления пароля работает **без необходимости настройки email сервиса**. Токены восстановления генерируются и отображаются прямо в интерфейсе приложения.

---

## 🔧 Как это работает

### 1. Запрос восстановления пароля

1. Пользователь переходит на страницу `/auth/forgot-password`
2. Вводит свой email
3. Нажимает "Send Reset Link"

### 2. Генерация токена

- Система генерирует безопасный токен восстановления
- Токен сохраняется в базе данных
- Срок действия токена: **1 час**

### 3. Отображение токена (без email)

**В development режиме** или **если email сервис не настроен**:

- Токен отображается прямо на странице
- Показывается полная ссылка для сброса пароля
- Можно скопировать токен или ссылку одним кликом
- Есть кнопка для перехода на страницу сброса пароля

---

## 🎨 Интерфейс

После запроса восстановления пароля пользователь видит:

```
🔧 Development Mode

Email service is not configured. Use the token below to reset your password.

Reset Link:
[Полная ссылка] [Копировать] [Перейти на страницу сброса]

Reset Token:
[Токен] [Копировать]
```

---

## ⚙️ Настройка

### Включение/выключение email сервиса

В файле `.env.local`:

```env
# Включить email сервис (когда будет настроен)
EMAIL_SERVICE_ENABLED=true

# Или оставить выключенным для работы без email
EMAIL_SERVICE_ENABLED=false
```

### Режимы работы

1. **Development режим** (`NODE_ENV=development`):
   - Токен всегда показывается в UI
   - Удобно для разработки и тестирования

2. **Production режим без email** (`NODE_ENV=production`, `EMAIL_SERVICE_ENABLED=false`):
   - Токен показывается в UI
   - Подходит для внутренних систем без email

3. **Production режим с email** (`NODE_ENV=production`, `EMAIL_SERVICE_ENABLED=true`):
   - Токен отправляется по email
   - Токен не показывается в UI (безопасность)

---

## 🔐 Безопасность

- ✅ Токены имеют срок действия (1 час)
- ✅ Токены можно использовать только один раз
- ✅ Rate limiting защищает от злоупотреблений
- ✅ Токены генерируются криптографически безопасным способом
- ✅ В production с email токены не показываются в UI

---

## 📝 Использование

### Для пользователя:

1. Запросить восстановление пароля
2. Скопировать ссылку или токен
3. Перейти по ссылке или вручную на `/auth/reset-password/[token]`
4. Ввести новый пароль

### Для разработчика:

```bash
# Запросить токен через API
curl -X POST http://localhost:3000/api/auth/forgot-password \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com"}'

# Ответ содержит токен (в dev режиме):
{
  "message": "...",
  "token": "abc123..."
}

# Использовать токен для сброса пароля
curl -X POST http://localhost:3000/api/auth/reset-password \
  -H "Content-Type: application/json" \
  -d '{
    "token": "abc123...",
    "password": "NewPassword123",
    "confirmPassword": "NewPassword123"
  }'
```

---

## 🚀 Миграция на email сервис

Когда будет настроен email сервис:

1. Установить переменную окружения:
   ```env
   EMAIL_SERVICE_ENABLED=true
   ```

2. Реализовать функцию отправки email в `src/app/api/auth/forgot-password/route.ts`:
   ```typescript
   await sendPasswordResetEmail({
     email: user.email,
     token,
     locale: (user.organization?.language as 'ru' | 'it') || 'it',
   })
   ```

3. Токены перестанут показываться в UI автоматически

---

## ✅ Преимущества

- ✅ Работает сразу без дополнительной настройки
- ✅ Удобно для разработки и тестирования
- ✅ Подходит для внутренних систем
- ✅ Легко мигрировать на email сервис
- ✅ Безопасно (токены с ограниченным сроком действия)

---

**Система готова к использованию без email сервиса!**

