# Проверка статуса приложения

## ✅ Что работает:

1. **Сервер запущен**: `http://localhost:3000`
2. **API endpoints отвечают**:
   - `/api/document-statuses?documentType=proposal` - возвращает статусы
   - `/api/document-statuses/[id]/set-default` - устанавливает основной статус
3. **Prisma Client обновлен** с новым полем `isDefault`
4. **Страницы загружаются**: главная, proposals, settings

## 🔍 Что нужно проверить:

1. **Откройте браузер** и перейдите на `http://localhost:3000`
2. **Откройте консоль браузера** (F12 → Console)
3. **Проверьте наличие ошибок** в консоли
4. **Попробуйте создать новое предложение** (`/proposals` → "Создать предложение")

## 📋 Если есть ошибка:

Пришлите:
- Текст ошибки из консоли браузера
- На какой странице возникает проблема
- Какое действие вызывает ошибку

